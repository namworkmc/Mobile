<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.studentmanagementapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.StudentManagementApp">
        <activity
                android:name=".Student.Activities.EditStudentInfoActivity"
                android:exported="false" />
        <activity
                android:name=".Student.Activities.StudentListActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".Student.Activities.AddStudentInfoActivity"
                android:exported="false">
        </activity>
    </application>

</manifest>